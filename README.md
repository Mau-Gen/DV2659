# DV2659 Repository for Assignment 1 and 2

### I will use git bash for this whole repo as to learn all commands necessary.

## Assignment 1

Assignment 1 – Agents, Graph, Graph
Searches and Implementation

The deadline for completing this assignment is the 20th of September 2026. You should submit
your work (e.g., zip file including your report, implementation etc.) on Canvas course webpage. Note that, the zip file should be named with your name and surname (e.g., name_surname.zip).
To pass this assignment, you must complete the following two questions successfully:
Problem 1: Shopping Assistant Agents (Not need to implement)

Problem 2: You will implement and compare different search algorithms (e.g., BFS and DFS) to find the shortest path in a complex 10x20 graph. You will analyze the efficiency, optimality, and performance of each algorithm.

Graph-based maze design: Find the original graph-based maze problem on the next page. For graph
representation, you are free to use any format (e.g., XML, JSON etc.).

Algorithm implementation: 
Implement the following search algorithms in Python:
• Breadth-First Search (BFS),
• Depth-First Search (DFS),
Pathfinding: Use each algorithm to find the shortest path from the start to the goal in the maze.

Performance Comparison: Compare the algorithms based on:
• Path optimality (shortest path)
• Path length (shortest path length): obtained by the algorithm

Visualization and Output: Create a visual representation of the graph and the paths found by each
algorithm in the implementation and report. Print the maze with the path from the start point to the goal point marked as '*’. If no path is found, indicate that no path exists.

Report: Write a short report discussing the implementation details, analyzing performance, and
discussing the strengths/weaknesses of each algorithm briefly. Provide insights into which algorithm is
most suitable for the maze problem.

Each algorithm will run in the implementation to find the shortest path between the starting point
(red ‘0’ in the maze) and the destination point (blue ‘0’ in the maze) in the maze. The program
should visually display the final path to the user. The maze consists of 10 rows and 20 columns,
where '1' should represent walls and '0' should represent free space in your graph
representation.
